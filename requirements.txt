# F2L - FTP to Local Multi-Endpoint Sync Application
# Optional dependencies for enhanced features

# System tray support
pystray>=0.19.0
Pillow>=8.0.0

# Scheduling support
schedule>=1.1.0

# Note: Core functionality works without these packages
# Core dependencies (included with Python):
# - tkinter (GUI)
# - sqlite3 (Database)
# - ftplib (FTP operations)
# - threading (Multi-threading)
# - concurrent.futures (Thread pool for scanning)
# - datetime, os, shutil, pathlib (File operations)

# Install optional dependencies with: pip install -r requirements.txt

# Features enabled by optional dependencies:
# - pystray + Pillow: System tray integration, minimize to tray
# - schedule: Advanced scheduling for auto-sync sessions

# New in v2.0:
# - Local-to-local sync (no additional dependencies)
# - Multi-session management (no additional dependencies)
# - Folder filtering (no additional dependencies)
# - Multi-threaded scanning (uses built-in concurrent.futures)
# - Scan caching (uses built-in sqlite3)
# - FTP directory browser (uses built-in tkinter + ftplib)
